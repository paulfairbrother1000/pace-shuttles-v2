// @vitest-environment jsdom
import React from 'react';import {cleanup,render,screen} from '@testing-library/react';import userEvent from '@testing-library/user-event';import {afterEach,describe,expect,it,vi} from 'vitest';import {AdminClockCalendar} from './admin-clock-calendar';afterEach(cleanup);
const model={dashboard:{control:{enabled:true,changed_at:'2026-09-13T12:00:00Z'},latest_run:{status:'completed',started_at:'2026-09-13T12:00:00Z',result:{emails:{sent:1,failed:0}}},audit:[]},events:[{event_key:'t24:b',booking_id:'b',departure_id:'d',route_name:'Jolly Harbour to Nobu',event_type:'t24',due_at:'2026-09-14T12:00:00Z',journey_timezone:'America/Antigua',execution_source:'scheduled',executed_at:null,status:'pending',failure_reason:null}]};
describe('AdminClockCalendar',()=>{it('shows control, real times and scheduled provenance',()=>{render(<AdminClockCalendar initial={model as any} request={vi.fn()}/>);expect(screen.getByText('Scheduler ON')).toBeTruthy();expect(screen.getByText('Jolly Harbour to Nobu')).toBeTruthy();expect(screen.getByText('Scheduled')).toBeTruthy();expect(screen.getByText(/America\/Antigua/)).toBeTruthy()});it('requires a pause reason and confirmation',async()=>{const request=vi.fn();render(<AdminClockCalendar initial={model as any} request={request}/>);await userEvent.setup().click(screen.getByRole('button',{name:'Turn scheduler off'}));expect(screen.getByRole('dialog')).toBeTruthy();expect(screen.getByLabelText('Reason for pausing')).toBeTruthy()})});
